
export default function ({ store, app, req }) {
  // let result = false;
  // if (process.client) {
  //   result = await store.dispatch('auth/tryAuthClient');
  // } else if (process.server) {
  //   result = await store.dispatch('auth/tryAuthServer', req);
  // }
  //
  // if (process.server) {
    // console.log(req, "req");
    // store.commit('auth/setToken', req.session.token)
  // }
  // setAxios(app.$axios);
  // console.log(`${process.client} ${process.server} @auth ${result}`);
  // if (!result || !store.state.auth.auth) {
  //   console.log('auth middleware');
  //   redirect('/auth/login');
  // }
}